import { WarehouseMaintenance } from './warehouse-maintenance';

describe('WarehouseMaintenance', () => {
  it('should create an instance', () => {
    expect(new WarehouseMaintenance()).toBeTruthy();
  });
});
